---
name: git-diff
description: Load current git changes as context, then perform a task
argument-hint: "What's the task to perform?"
disable-model-invocation: true
---

Load the current git changes as context, including untracked files, then perform the following task: $ARGUMENTS
